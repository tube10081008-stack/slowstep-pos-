# Vercel(서버리스) 빌드용 의존성. Vercel은 루트 디렉터리(slowstep-pos)의
# 이 파일을 설치한다. (-r 포함 구문은 Vercel 파서가 못 읽으므로 직접 나열)
Django>=5.0
djangorestframework>=3.15
django-cors-headers>=4.3
dj-database-url>=2.2
psycopg[binary]>=3.2
whitenoise>=6.7
