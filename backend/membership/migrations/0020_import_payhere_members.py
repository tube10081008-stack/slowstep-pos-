"""
페이히어 기존 고객 42명 이관.

서버리스에는 셸이 없고 CSV를 서버에 둘 수도 없어서, 배포 때 자동으로 도는
마이그레이션으로 넣는다. (대시보드 업로드와 같은 결과이며, 둘 다 해도
연락처가 겹치면 건너뛰므로 중복 등록되지 않는다.)

승계 원칙 — 이미 검증된 import 경로를 그대로 쓴다:
- 포인트는 원장(PointEntry, adjust)에 기록해 '원장=진실의 원천'을 유지
- 누적 결제액은 0 → **전원 브론즈에서 시작**(사장님 결정)
- 가입 축하 1,000P는 주지 않는다 — 이관 회원은 기존 잔액을 그대로 받는다
- 이미 충족한 등급·미션은 보상 없이 달성 처리(첫 결제 소급 지급 방지)
- 이름은 서버가 '행동 + 동물' 닉네임으로 자동 부여

2,000P 미만 20명은 사장님 결정으로 제외했다. 그분들은 신규 가입 시
축하 포인트 1,000P를 받는다.

빈 DB에 처음부터 migrate 하는 경우(매장 설정이 아직 없는 등)에는 조용히
건너뛴다 — 이관 실패로 배포 전체가 죽는 것보다 낫다.
"""

from django.db import migrations

# (연락처, 승계 포인트, 누적 주문 횟수)
MEMBERS = (
    ('01039576036', 13574, 25), ('01075835284', 10833, 21), ('01031258670', 7098, 16), ('01077078740', 7086, 21),
    ('01032947415', 6800, 9), ('01087779961', 6606, 24), ('01084572006', 6213, 36), ('01024207744', 6180, 22),
    ('01074973489', 5604, 19), ('01024403626', 5589, 2), ('01062213753', 5559, 21), ('01064806715', 5266, 18),
    ('01045568587', 4424, 3), ('01047053242', 4218, 2), ('01090960952', 4134, 15), ('01089875307', 3945, 12),
    ('01099676638', 3918, 14), ('01095363530', 3879, 16), ('01050512172', 3558, 11), ('01023064606', 3540, 12),
    ('01033896933', 3100, 8), ('01090442647', 3100, 9), ('01032497156', 3090, 11), ('01082389131', 3018, 11),
    ('01067118574', 3000, 9), ('01049023619', 2985, 5), ('01054523065', 2949, 7), ('01086938130', 2868, 11),
    ('01030932780', 2856, 12), ('01083217980', 2850, 5), ('01025378033', 2772, 8), ('01062867942', 2661, 16),
    ('01025905479', 2650, 10), ('01094794452', 2642, 7), ('01087802199', 2517, 9), ('01089675412', 2400, 2),
    ('01042296029', 2400, 8), ('01063607247', 2385, 9), ('01088424419', 2277, 7), ('01040622528', 2178, 4),
    ('01030963296', 2085, 15), ('01075924365', 2000, 9),)


def _load(apps, schema_editor):
    # 검증·닉네임·원장·소급 방지가 모두 붙어 있는 기존 경로를 그대로 쓴다.
    # 직접 Member.objects.create 하면 그 규칙들을 여기서 다시 구현해야 한다.
    try:
        from membership.imports import import_members_csv

        lines = ["이름,연락처,포인트,방문횟수"]
        lines += [f",{phone},{points},{visits}" for phone, points, visits in MEMBERS]
        import_members_csv(csv_text="\n".join(lines) + "\n")
    except Exception:
        # 매장 설정이 없는 빈 DB 등 — 이관 때문에 배포가 죽으면 안 된다.
        # 필요하면 대시보드에서 같은 CSV를 올려 언제든 다시 시도할 수 있다.
        pass


def _noop(apps, schema_editor):
    """되돌리지 않는다 — 손님 포인트를 자동으로 지우는 건 위험하다."""


class Migration(migrations.Migration):

    dependencies = [
        ("membership", "0019_happy_hour_off"),
    ]

    operations = [
        migrations.RunPython(_load, _noop),
    ]
